const IP = `localhost`;
const PORT = `50541`;
const PLAYER = `HSC`;

const keyMapping = {
  w: 'Move: up',
  a: 'Move: left',
  s: 'Move: down',
  d: 'Move: right',
  g: 'Say: Hi',
  t: 'Say: :(',
  c: 'Say: not bad!',
}

module.exports = {
  IP,
  PORT,
  PLAYER,
  keyMapping
};

